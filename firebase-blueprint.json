{
  "entities": {
    "settings": {
      "title": "Global Settings",
      "description": "Stores admin PIN, buletin config, and global dashboard configuration",
      "type": "object",
      "properties": {
        "adminPin": { "type": "string" },
        "dashboardConfig": { "type": "object" },
        "buletinConfig": { "type": "object" },
        "updatedAt": { "type": "string" }
      }
    },
    "satkerData": {
      "title": "Satker Data List",
      "description": "Stores list of satkers with IKPA, realisasi belanja, and Capaian Output scores",
      "type": "object",
      "properties": {
        "list": { "type": "array" },
        "records": { "type": "array" },
        "updatedAt": { "type": "string" }
      }
    },
    "pejabatData": {
      "title": "Pejabat Sertifikasi Data",
      "description": "Stores list of certified perbendaharaan officials",
      "type": "object",
      "properties": {
        "list": { "type": "array" },
        "updatedAt": { "type": "string" }
      }
    },
    "presensiData": {
      "title": "Presensi Peserta Data",
      "description": "Stores online attendance entries for KPPN socialization sessions",
      "type": "object",
      "properties": {
        "list": { "type": "array" },
        "updatedAt": { "type": "string" }
      }
    },
    "announcements": {
      "title": "Announcements",
      "description": "Stores official announcements and circulars for satkers",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "content": { "type": "string" },
        "date": { "type": "string" },
        "category": { "type": "string" },
        "isPinned": { "type": "boolean" },
        "author": { "type": "string" }
      },
      "required": ["title", "content", "date"]
    },
    "simulationProject": {
      "title": "IKPA Simulation Project",
      "description": "Stores real-time multi-indicator simulation projects and active state shared across development and deployment",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "updatedAt": { "type": "string" },
        "lastModifiedBy": { "type": "string" },
        "origin": { "type": "string" },
        "projectData": { "type": "object" },
        "activeProjectId": { "type": "string" }
      }
    },
    "catatanDiskusiSatker": {
      "title": "Catatan Diskusi KPPN & Satker (Khusus Admin)",
      "description": "Stores history and notes of consultations, discussions, and meetings between KPPN officers and Satker representatives",
      "type": "object",
      "properties": {
        "kodeSatker": { "type": "string" },
        "namaSatker": { "type": "string" },
        "list": { "type": "array" },
        "updatedAt": { "type": "string" }
      }
    },
    "pendaftaranUserSakti": {
      "title": "Pendaftaran Pengguna SAKTI",
      "description": "Stores registration drafts and submissions of SAKTI users per satker",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "kodeSatker": { "type": "string" },
        "namaSatker": { "type": "string" },
        "levelSatker": { "type": "string" },
        "isBLU": { "type": "boolean" },
        "users": { "type": "array" },
        "status": { "type": "string" },
        "createdBy": { "type": "string" },
        "updatedAt": { "type": "string" },
        "exportedAt": { "type": "string" }
      },
      "required": ["kodeSatker", "namaSatker", "users"]
    },
    "haicsoTickets": {
      "title": "HAICSO Tickets",
      "description": "Stores normalized ticket records from HAICSO Excel uploads",
      "type": "object",
      "properties": {
        "list": { "type": "array" },
        "totalRecords": { "type": "number" },
        "updatedAt": { "type": "string" }
      }
    },
    "haicsoUploadBatch": {
      "title": "HAICSO Upload Batch",
      "description": "Stores upload batch metadata and audit records for HAICSO Excel imports",
      "type": "object",
      "properties": {
        "list": { "type": "array" },
        "updatedAt": { "type": "string" }
      }
    },
    "haicsoDashboardSettings": {
      "title": "HAICSO Dashboard Settings",
      "description": "Stores active status, toggle, and configuration for Monitoring Tiket HAICSO",
      "type": "object",
      "properties": {
        "isActive": { "type": "boolean" },
        "updatedBy": { "type": "string" },
        "updatedAt": { "type": "string" }
      }
    },
    "gajiIndukData": {
      "title": "Monitoring SPM Gaji Induk Data",
      "description": "Stores records and upload batches of SPM Gaji Induk (PNS & PPPK)",
      "type": "object",
      "properties": {
        "records": { "type": "array" },
        "uploads": { "type": "array" },
        "updatedAt": { "type": "string" }
      }
    },
    "kontrakMonitoringData": {
      "title": "Monitoring Data Kontrak",
      "description": "Stores records and upload batches of Monitoring Data Kontrak KPPN",
      "type": "object",
      "properties": {
        "records": { "type": "array" },
        "uploads": { "type": "array" },
        "updatedAt": { "type": "string" }
      }
    },
    "userData": {
      "title": "KPPN User Accounts",
      "description": "Stores user accounts for Super Admin and Pegawai KPPN with roles, greetings, and Google Drive photos",
      "type": "object",
      "properties": {
        "users": { "type": "array" },
        "updatedAt": { "type": "string" }
      }
    }
  },
  "firestore": {
    "/data/users": {
      "schema": { "$ref": "#/entities/userData" },
      "description": "Centralized sync dataset for Super Admin and Pegawai user accounts"
    },
    "/settings/{settingId}": {
      "schema": { "$ref": "#/entities/settings" },
      "description": "Global application configuration documents"
    },
    "/data/{dataType}": {
      "schema": { "$ref": "#/entities/satkerData" },
      "description": "Centralized sync datasets for Satker IKPA, Realisasi Belanja, Pejabat, and Presensi"
    },
    "/data/gaji_induk": {
      "schema": { "$ref": "#/entities/gajiIndukData" },
      "description": "Centralized sync dataset for SPM Gaji Induk records and batches"
    },
    "/data/kontrak_monitoring": {
      "schema": { "$ref": "#/entities/kontrakMonitoringData" },
      "description": "Centralized sync dataset for Monitoring Data Kontrak records and batches"
    },
    "/settings/haicso": {
      "schema": { "$ref": "#/entities/haicsoDashboardSettings" },
      "description": "HAICSO dashboard visibility and target configuration"
    },
    "/data/haicso_tickets": {
      "schema": { "$ref": "#/entities/haicsoTickets" },
      "description": "Centralized sync dataset for HAICSO tickets"
    },
    "/data/haicso_batches": {
      "schema": { "$ref": "#/entities/haicsoUploadBatch" },
      "description": "Centralized sync dataset for HAICSO upload batches"
    },
    "/announcements/{announcementId}": {
      "schema": { "$ref": "#/entities/announcements" },
      "description": "Public announcements and bulletin notices"
    },
    "/simulasi_projects/{projectId}": {
      "schema": { "$ref": "#/entities/simulationProject" },
      "description": "Real-time synchronized simulation projects across dev and deployment instances"
    },
    "/simulasi_state/{stateId}": {
      "schema": { "$ref": "#/entities/simulationProject" },
      "description": "Active simulation project state and active ID synchronization"
    },
    "/catatan_diskusi_satker/{kodeSatker}": {
      "schema": { "$ref": "#/entities/catatanDiskusiSatker" },
      "description": "Private KPPN-Satker consultation records and action item tracking"
    },
    "/pendaftaran_user_sakti/{draftId}": {
      "schema": { "$ref": "#/entities/pendaftaranUserSakti" },
      "description": "SAKTI user registration drafts and submission records"
    }
  }
}

